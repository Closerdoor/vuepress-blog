{
  "name": "wwm",
  "version": "1.0.0",
  "description": "a blog",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs --cache .cache",
    "docs:build": "vuepress build docs",
    "deploy": "bash deploy.sh"
  },
  "keywords": [
    "blog"
  ],
  "author": "closerdoor",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^1.9.7",
    "vuepress-theme-reco": "^1.6.10"
  }
}
